# Rudrastra B2B Technical Marketplace & Engineering Discovery Engine
# Comprehensive Project Context, Architecture v1.0 Blueprint & Codebase State for ChatGPT

---

## 1. Executive Summary & Product Vision

Rudrastra is India's canonical **Technical B2B Marketplace + Product Information System (PIM) + Engineering Discovery Engine** for Drone Hardware.
It is built specifically for drone manufacturers, defense contractors, robotics companies, and component suppliers in India.

### Core Value Moat
- **Not just a storefront**: A structured technical catalog and engineering compatibility graph. Multi-vendor commerce is the monetization layer built on top of authoritative engineering data.
- **McMaster-Carr style PIM Architecture**: Category-specific typed specifications, units of measurement, MPN identity, CAD model attachments, and serial-verified diagnostics.
- **Dual User Experience**:
  - Storefront & Discovery: Minimal, high-performance, visual Uncover design aesthetic (`#F5F5F5` background, `#111111` typography, Saffron/Navy/Green engineering accents).
  - Engineering & B2B Workspace: Dense, structured technical information (datasheets, CAD models, MPNs, specifications, compatibility trees, lead times, certifications, credit lines).

---

## 2. Frozen Architecture v1.0 & The Eight Commandments

The system architecture is **FROZEN v1.0**. All code modifications must strictly obey these 8 commandments:

1. **`Product ≠ Variant ≠ Seller Offer ≠ Seller SKU ≠ Inventory`**:
   - `catalog_products`: Canonical manufacturer product family (e.g. "T-Motor MN4014").
   - `product_variants`: Sellable engineering variations (e.g. "400KV", "500KV").
   - `seller_offers`: Commercial offer (price, lead time, MOQ) by seller.
   - `seller_skus`: Seller SKU mapping to an offer (`UNIQUE(seller_id, sku_code)`).
   - `inventory_items`: Physical stock tracking by location and seller SKU.
2. **`PostgreSQL is Source of Truth; Typesense is Derived Index`**:
   - Supabase PostgreSQL handles all relational integrity, transactions, and audit logs.
   - Typesense is derived and disposable, reconstructible via `/api/admin/search/rebuild`.
3. **`Technical Specifications are Structured, Typed, Normalized PIM Data`**:
   - Categories -> `spec_definitions` -> `product_spec_values` (linked to variant or product).
4. **`Inventory and Money Movement are Transactional, Idempotent, Auditable`**:
   - `sellable_quantity = on_hand_quantity - reserved_quantity`. Enforce 10-minute TTL reservations.
   - Double-entry immutable ledger (`ledger_transactions`, `ledger_accounts`, `ledger_entries`).
5. **`Every Tenant Boundary is Enforced by RLS + Auth`**:
   - Public/User context connects via JWT with RLS policies (`USING` + `WITH CHECK`).
6. **`Every State Transition is CQRS-Enforced & Replay-Safe`**:
   - Commands own mutations within transactions. Queries never mutate state. Webhooks processed idempotently via `processed_webhooks`.
7. **`Outbox Delivery is At-Least-Once; Event Consumers Must Be Idempotent`**:
   - `outbox_events` guarantees at-least-once delivery with exponential backoff.
8. **`Least-Privilege Worker Capability Model`**:
   - Workers assigned explicit `WORKER_CAPABILITIES` (`SEARCH_INDEX_WRITE`, `OUTBOX_PROCESS`, etc.).

---

## 3. Design System & Universal UI/UX Guidelines

- **Background Canvas**: `#F5F5F5`
- **Primary Foreground / Text**: `#111111`
- **Cards / Containers**: `#FFFFFF` with `#E5E5E5` borders
- **Brand Engineering Accents**:
  - Primary (Saffron): `#F35C27`
  - Accent (Navy): `#305CDE`
  - Success (Green): `#138808`
- **Typography**: Syne (Headings), Inter (Body & UI), Font-Mono (SKUs, Prices, AWBs, GSTINs).
- **Universal Top Header Navigation (`Navbar.tsx`)**:
  `[ Logo ]  [ Categories  Products  Engineering  Manufacturers ]  [cart] [ Request Quote (Black Box) ] [ Auth / Profile ]`
- **Session Authority & Role-Based Security**:
  - Fixed Session Roles for Standard Users: Customer sees Customer links; Seller sees Seller links; Ops sees Ops links.
  - Developer Testing Mode: Role switcher badge (`Dev Role Override`) rendered exclusively for `praneeth@vyooma.tech` to allow multi-tenant UI testing during development.
  - Unauthenticated Visitors: Display explicit `Sign In` (`/login`) and `Create Account` (`/signup`) buttons in header.

---

## 4. Completed 40 Page Route Inventory

All 40 routes compile cleanly via Next.js 16 Turbopack compiler (`npm run build` verified):

### Authentication & Merchant Onboarding (3 Routes)
- `/login` — Sign In to Rudrastra (Email/Password, Corporate SSO)
- `/signup` — Create Account (Customer Engineer vs Seller Merchant Registration)
- `/sell` — Become a Verified Seller Onboarding Portal

### Storefront & Product Discovery (11 Routes)
- `/` — Storefront Home
- `/categories` — Taxonomy Directory
- `/categories/[category]` — Parametric Category Filter Page
- `/products` — Canonical Product Catalog
- `/products/[id]` — Product Detail Page (PDP) & Seller Offer Comparison Table
- `/engineering` — Engineering Datasheets & CAD Library
- `/manufacturers` — Canonical Brand Index
- `/compatibility` — Compatibility Explorer
- `/compare` — Side-by-Side Component Comparison
- `/bom` — Bulk BOM Import & RFQ Tool
- `/rfq` — B2B Request for Quote Form

### Customer Account Suite (6 Routes)
- `/account` — Engineering Account Overview
- `/account/orders` — Order History & Granular Tracking
- `/account/orders/[id]` — Order Detail & FSM Progress Timeline
- `/account/rma` — Technical RMA Diagnostic Evidence Submission
- `/account/warranties` — Active Warranties & Coverage Policies
- `/account/tickets` — Support Tickets & SLA Timers

### B2B Organization Workspace (4 Routes)
- `/organization/dashboard` — B2B Spend Metrics & Approved Net Terms Credit
- `/organization/projects` — Engineering Drone Projects & BOM Allocation
- `/organization/members` — Team Members & PO Approval Limits
- `/organization/procurement` — Procurement & PO Sign-Off Workflow

### Seller Merchant Hub (5 Routes)
- `/seller/dashboard` — Merchant Sales & Fulfillment Overview
- `/seller/offers` — Offer Mapping, Prices & GST Rates
- `/seller/inventory` — Stock Ledger (On-hand vs Reserved)
- `/seller/orders` — Sub-Orders & Shiprocket AWB Dispatch
- `/seller/payouts` — Razorpay Route Settlements & Fees

### Operations Mission Control (7 Routes)
- `/ops` — Ops Overview & Superadmin Control
- `/ops/catalog` — PIM Catalog Verification Desk
- `/ops/tickets` — Technical Support Desk & SLA Triage
- `/ops/rma` — RMA Quarantine & Serial Verification Desk
- `/ops/disputes` — Buyer/Seller Dispute Arbitration
- `/ops/reconciliation` — Financial Double-Entry Ledger Reconciliation
- `/ops/audit-logs` — Immutable System Audit Trail

### Cart, Checkout & Support (4 Routes)
- `/cart` — Procurement Cart & Compatibility Check
- `/checkout` — Corporate GST Checkout & Net Terms Selection
- `/checkout/success` — Order Confirmation & Split Seller Emission
- `/support` — Technical Diagnostic & Support Portal

---

## 5. Canonical Header Component (`src/components/layout/Navbar.tsx`)

```tsx
"use client";

import React, { useState } from "react";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { 
  ShoppingCart, User, Package, Wrench, ShieldCheck, 
  Store, Building2, ChevronDown, Menu, X, Tag, Layers, Landmark,
  LogIn, UserPlus, LogOut, ShieldAlert, Code2
} from "lucide-react";

export function Navbar() {
  const pathname = usePathname();
  const [isProfileOpen, setIsProfileOpen] = useState(false);
  const [isMobileMenuOpen, setIsMobileMenuOpen] = useState(false);
  
  const currentUser = {
    name: "Praneeth Kumar",
    email: "praneeth@vyooma.tech",
  };

  const [sessionRole, setSessionRole] = useState<"customer" | "seller" | "ops" | "logged_out">("customer");

  const centerNavLinks = [
    { name: "Categories", path: "/categories" },
    { name: "Products", path: "/products" },
    { name: "Engineering", path: "/engineering" },
    { name: "Manufacturers", path: "/manufacturers" },
  ];

  return (
    <header className="sticky top-0 z-50 w-full bg-white/90 backdrop-blur-md border-b border-[var(--border)] font-sans">
      <nav className="w-full px-6 lg:px-10 h-[72px] flex items-center justify-between">
        <Link href="/" className="flex items-center gap-2 shrink-0">
          <svg width="36" height="28" viewBox="0 0 36 28" fill="none" className="text-[var(--foreground)]">
            <path d="M0 0L12 14L0 28H6L18 14L6 0H0Z" fill="currentColor" />
            <path d="M14 0L26 14L14 28H20L32 14L20 0H14Z" fill="currentColor" />
          </svg>
          <span className="font-heading font-bold text-[16px] tracking-tight ml-2">RUDRASTRA</span>
        </Link>

        <div className="hidden md:flex items-center gap-1 font-medium text-[14px]">
          {centerNavLinks.map((link) => (
            <Link key={link.name} href={link.path} className="px-4 py-2 border rounded-full hover:border-[#111111] transition-colors">
              {link.name}
            </Link>
          ))}
        </div>

        <div className="flex items-center gap-3">
          <Link href="/cart" className="p-2 relative" title="Shopping Cart">
            <ShoppingCart className="w-5 h-5" />
            <span className="absolute top-0 right-0 bg-[#111111] text-white font-mono text-[10px] font-bold w-4 h-4 rounded-full flex items-center justify-center">3</span>
          </Link>

          <Link href="/rfq" className="hidden md:inline-flex px-5 py-2.5 bg-[#111111] text-white text-[13px] font-semibold">
            Request Quote
          </Link>

          {sessionRole === "logged_out" ? (
            <div className="hidden sm:flex items-center gap-2 text-xs font-semibold">
              <Link href="/login" className="px-3.5 py-2 hover:bg-slate-100 flex items-center gap-1.5"><LogIn className="w-4 h-4" /> Sign In</Link>
              <Link href="/signup" className="px-4 py-2 border border-[#111111] flex items-center gap-1.5"><UserPlus className="w-4 h-4" /> Create Account</Link>
            </div>
          ) : (
            <div className="relative">
              <button onClick={() => setIsProfileOpen(!isProfileOpen)} className="p-2 flex items-center gap-1.5">
                <User className="w-5 h-5" />
                <ChevronDown className="w-3.5 h-3.5 text-slate-500" />
              </button>

              {isProfileOpen && (
                <div className="absolute right-0 mt-2 w-72 bg-white border rounded-xl shadow-xl p-3 z-50 text-[13px]">
                  <div className="p-3 bg-slate-50 rounded border mb-2 font-bold">{currentUser.name} ({sessionRole.toUpperCase()})</div>

                  {/* DEVELOPER TESTING SWITCHER — EXCLUSIVELY FOR praneeth@vyooma.tech */}
                  {currentUser.email === "praneeth@vyooma.tech" && (
                    <div className="bg-amber-50 border border-amber-200 p-2 rounded mb-3 text-[10px]">
                      <div className="font-bold text-amber-800 uppercase flex items-center gap-1"><Code2 className="w-3 h-3" /> Dev Role Override</div>
                      <div className="grid grid-cols-3 gap-1 mt-1 text-center font-semibold">
                        <button onClick={() => setSessionRole("customer")} className={`py-1 rounded ${sessionRole === "customer" ? "bg-amber-600 text-white font-bold" : "bg-white text-amber-900 border"}`}>Customer</button>
                        <button onClick={() => setSessionRole("seller")} className={`py-1 rounded ${sessionRole === "seller" ? "bg-amber-600 text-white font-bold" : "bg-white text-amber-900 border"}`}>Seller</button>
                        <button onClick={() => setSessionRole("ops")} className={`py-1 rounded ${sessionRole === "ops" ? "bg-amber-600 text-white font-bold" : "bg-white text-amber-900 border"}`}>Ops</button>
                      </div>
                    </div>
                  )}

                  {/* FIXED CUSTOMER ROLE LINKS */}
                  {sessionRole === "customer" && (
                    <div className="space-y-1">
                      <Link href="/account" onClick={() => setIsProfileOpen(false)} className="block p-2 hover:bg-slate-50 font-semibold">Account Dashboard</Link>
                      <Link href="/account/orders" onClick={() => setIsProfileOpen(false)} className="block p-2 hover:bg-slate-50 font-semibold">Orders & Tracking</Link>
                      <Link href="/sell" onClick={() => setIsProfileOpen(false)} className="block p-2 hover:bg-emerald-50 text-emerald-700 font-bold">Become a Seller</Link>
                    </div>
                  )}

                  {/* FIXED SELLER ROLE LINKS */}
                  {sessionRole === "seller" && (
                    <div className="space-y-1">
                      <Link href="/seller/dashboard" onClick={() => setIsProfileOpen(false)} className="block p-2 text-emerald-700 font-semibold">Seller Dashboard</Link>
                      <Link href="/seller/offers" onClick={() => setIsProfileOpen(false)} className="block p-2 text-emerald-700 font-semibold">Offers & SKU Mapping</Link>
                    </div>
                  )}

                  <div className="border-t mt-2 pt-2">
                    <button onClick={() => { setSessionRole("logged_out"); setIsProfileOpen(false); }} className="w-full text-left p-2 text-rose-600 font-bold flex items-center gap-2">
                      <LogOut className="w-4 h-4" /> Sign Out of Session
                    </button>
                  </div>
                </div>
              )}
            </div>
          )}
        </div>
      </nav>
    </header>
  );
}
```

---

## 6. Backend Domain Execution Roadmap (Phases 5 → 15)

Frontend UI is 100% complete with authentication screens, fixed user session roles, and exclusive developer testing controls for `praneeth@vyooma.tech`. Next steps involve building out backend domain handlers in `src/modules/` following the 16-step Definition of Done (DoD).
